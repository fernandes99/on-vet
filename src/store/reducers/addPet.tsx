import { createSlice } from "@reduxjs/toolkit";

const addPet = createSlice({
    name: 'addPet',
    initialState: {
        
    },
    reducers: {

    }
})

export const {  } = addPet.actions;
export default addPet.reducer;